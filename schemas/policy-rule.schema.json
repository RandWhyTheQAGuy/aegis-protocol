{
  "rule_id":               string,   // Human-readable ID
  "description":           string,   // Detailed purpose of the rule
  "trust": {
    "min_authority_confidence":   float, // Threshold to consider score valid
    "min_sensitivity_confidence": float  // Threshold to consider score valid
  },
  "scope": {
    "min_authority":       float?,   // Optional: Required authority level
    "max_sensitivity":     float?    // Optional: Maximum allowed sensitivity
  },
  "action":                string,   // Enum: ["ALLOW", "DENY", "FLAG"]
  "log_level":             string    // Enum: ["INFO", "WARN", "ALERT"]
}