from setuptools import setup, find_packages

setup(
    name="aegis_protocol",
    version="0.1.0",
    description="Semantic Agent Security SDK — Python port of the Aegis Protocol C++ reference implementation",
    packages=find_packages(),
    python_requires=">=3.9",
)
